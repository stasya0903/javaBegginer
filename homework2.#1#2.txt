1 c = ++a; alert(c);           // 2
 В префиксной форме инкрементирование производится сразу, а возврат — уже с обновленным значением. Соответственно c= a+1= 2
d = b++; alert(d);           // 1
В постфиксной форме сначала происходит возвращение значения,
соответственно d= b= 1, но если мы вызовем переменную b после этого она будет равна 2
c = (2+ ++a); alert(c); 
поскольку a = 2 после бредущего инкрементирования , с очередным префиксом инкрементированием  возвращается значение 3, 2+3=5
d = (2+ b++); alert(d);
поскольку после инкрементирования b=2, то d=2+2, а переменная b увеличиться на 2 после
alert(a);                    // 3 alert(b);                    // 3
поскольку после инкрементирования  (дважды) обе переменные  переменны увеличились на 2 их значение равно 3
2 Чему будет равен x?  
var a = 2; var x = 1 + (a *= 2);
x=5
